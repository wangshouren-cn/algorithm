/* const sort_fn = require('./functions/12.js')
const sort_number = require('./对数器/sort_number')
const size = 10,
  value = 100,
  count = 10000
sort_number(size, value, sort_fn, count) */

/* const TestStackOrQueue = require('./对数器/TestStackOrQueue')
const Queue = require('./utils/Queue')
const TwoStacksQueue = require('./functions/12')
new TestStackOrQueue(
  1000,
  10,
  100,
  Queue,
  TwoStacksQueue,
  ['add', 'poll', 'peek'],
  'add'
).start() */

/* const test = require('./对数器/13')
const reverse = require('./functions/13')
test(
  10,
  10,
  1000,
  (stack) => {
    stack.stack.reverse()
  },
  reverse
) */
